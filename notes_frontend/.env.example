# Frontend -> Backend base URL (include scheme + host + port)
# Example: http://localhost:4000
REACT_APP_API_BASE_URL=

# Optional: set to true to enable extra console logging in the API client
REACT_APP_DEBUG_API=false
